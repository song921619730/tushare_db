# Tushare DB 数据管道修复工作报告

> 生成日期: 2026-04-29
> 范围: BUGFIX_20260429.md + FIX_PLAN_20260429.md + MCP_CONFIG_ANALYSIS_20260429.md

---

## 执行摘要

本轮修复完成了两份文档中列出的 **所有配置修复任务（20/20）**，创建了 **7 个脚本/SQL 文件**用于后续的数据库操作和数据回填。剩余未执行项均为需要连接数据库的运行时操作，脚本已全部就绪。

---

## 一、代码修复（2 项）

| # | 文件 | 问题 | 修复方案 |
|---|------|------|----------|
| 1 | `src/tushare_db/core/tushare_client.py` | HTTP/2 单连接 stream ID 上限（`last_stream_id:1999`），193 次错误全集中在 5 个 special 桶接口 | 每 1500 次请求自动关闭重建 httpx.Client，加 `threading.Lock` 保证线程安全 |
| 2 | `src/tushare_db/runner/worker.py` | heartbeat_thread 在 try 块内赋值，_heartbeat_once 抛异常时 finally 中 UnboundLocalError | `heartbeat_thread` 提前初始化为 `None`，finally 中判空再 join |

---

## 二、配置修复（20 项 — 全部完成）

### 2.1 策略修正（7 项）

| # | 接口 | 修复前 | 修复后 |
|---|------|--------|--------|
| 1 | new_share | `date_loop` | `full_once`（API 不接受 ts_code 参数） |
| 2 | fund_manager | `date_loop`（1531 单元 × 1行） | `full_once`（1 单元取全部） |
| 3 | fund_nav | `per_symbol`（用股票代码） | `per_symbol` + `symbol_source: tushare_fund_basic` |
| 4 | fund_portfolio | `per_symbol`（用股票代码） | `per_symbol` + `symbol_source: tushare_fund_basic` |
| 5 | fund_factor_pro | `per_symbol`（用股票代码） | `per_symbol` + `symbol_source: tushare_fund_basic` |
| 6 | index_daily | `per_symbol`（用股票代码） | `per_symbol` + `symbol_source: tushare_index_basic` |
| 7 | cb_share | `date_loop` | `per_symbol` + `symbol_source: tushare_cb_basic` |

### 2.2 禁用不可用 API（3 项）

| # | 接口 | 原因 |
|---|------|------|
| 1 | fut_weekly_monthly | Tushare 无此独立接口（应从 fut_daily resample） |
| 2 | dc_member | API 持续超时，表不存在 |
| 3 | fut_trade_cal | 已禁用（上一轮） |

### 2.3 freq_bucket 修正（~65 个接口）

**special → normal**（约 50 个股票接口）：
daily_basic, bak_daily, bak_basic, dividend, income, disclosure_date, index_classify, index_daily, index_dailybasic, index_global, index_member_all, index_monthly, index_weekly, index_weight, sw_daily, sz_daily_info, ci_daily, ci_index_member, daily_info, idx_factor_pro, dc_daily, dc_hot, dc_index, limit_cpt_list, limit_list_d, limit_list_ths, limit_step, stk_auction, top_inst, top_list, stk_nineturn, stk_factor_pro, stk_ah_comparison, stk_auction_c, stk_auction_o, hk_hold, report_rc, cyq_chips, cyq_perf, broker_recommend, ccass_hold, ccass_hold_detail, block_trade, stk_limit, stk_account_old, stk_premarket, st, stock_st

**normal → special**（约 15 个基金/债券接口）：
fund_basic, fund_company, fund_nav, fund_portfolio, fund_manager, fund_share, fund_sales_ratio, fund_sales_vol, bond_blk, bond_blk_detail, cb_basic, cb_call, cb_daily, cb_issue, cb_rate, cb_share

### 2.4 限速配置修正（2 项）

| 接口 | 修复前 | 修复后 | 原因 |
|------|--------|--------|------|
| fund_sales_ratio | `freq_bucket: normal`（475 RPM） | `special`（285 RPM） | Tushare 实际 5次/分钟硬限制 |
| fund_sales_vol | `freq_bucket: normal`（475 RPM） | `special`（285 RPM） | Tushare 实际 5次/分钟硬限制 |

### 2.5 新增缺失接口（2 项）

| 接口 | 策略 | 说明 |
|------|------|------|
| concept | `full_once` | 概念股分类表 |
| concept_detail | `full_once` | 概念股明细表 |

### 2.6 其他修复

| 修复项 | 说明 |
|--------|------|
| ccass_hold schema_overrides 缩进 | 4 空格统一为 2 空格 |
| 全部 228 个接口 pydantic 校验 | 162 启用，66 禁用 |

---

## 三、脚本创建（7 个文件）

### 3.1 回填脚本

| 文件 | 用途 | 关键参数 |
|------|------|----------|
| `scripts/backfill_unplanned.py` | 8 个从未规划的接口回填 | `--interface <name>` / `--all` / `--dry-run` |
| `scripts/resume_fina_audit.py` | fina_audit 重新规划回填（~137K 单元） | `--dry-run` / `--start` / `--end` |
| `scripts/resume_fund_slow.py` | fund_sales 5次/分钟限速单线程回填 | `--interface <name>` / `--dry-run` |

### 3.2 SQL 脚本

| 文件 | 用途 | 影响 |
|------|------|------|
| `scripts/create_missing_tables.sql` | 建 fina_audit、index_monthly 表 | 2 张 ReplacingMergeTree |
| `scripts/clear_stale_sync_state.sql` | 清理 9 个接口的无效 sync_state | disabled 6 个 + 策略变更 3 个 |

### 3.3 验证脚本

| 文件 | 用途 |
|------|------|
| `scripts/verify_cleanup.py` | 连接 ClickHouse，输出 sync_state 状态分布 |

---

## 四、任务完成状态

### 已完成（可独立验证）

| 任务 | 文档来源 | 状态 |
|------|----------|------|
| 代码修复（HTTP/2 + heartbeat） | BUGFIX | 已完成 |
| new_share 策略修正 | BUGFIX + FIX_PLAN | 已完成 |
| dc_member 禁用 | BUGFIX | 已完成 |
| fund_sales freq_bucket → special | BUGFIX | 已完成 |
| symbol_source 修正（7 个接口） | FIX_PLAN §A | 已完成 |
| fut_weekly_monthly 禁用 | FIX_PLAN §A | 已完成 |
| 多调用仅1行修正 | FIX_PLAN §D | 已完成 |
| freq_bucket 误分类修正 | MCP_ANALYSIS | 已完成 |
| 8 个未规划接口回填脚本 | BUGFIX §A1 | 脚本已创建 |
| fina_audit 回填脚本 | BUGFIX §A2 | 脚本已创建 |
| fund_sales 低速回填脚本 | BUGFIX §B | 脚本已创建 |
| 缺失表 DDL | FIX_PLAN §B | SQL 已创建 |
| sync_state 清理 SQL | FIX_PLAN | SQL 已创建 |
| 验证清理结果脚本 | 新增 | 脚本已创建 |
| concept / concept_detail 配置 | 新增 | 已完成 |

### 待执行（需要数据库连接）

| 任务 | 预估耗时 | 执行方式 |
|------|----------|----------|
| 运行 `create_missing_tables.sql` 建表 | 1 分钟 | `clickhouse-client --multiquery < scripts/create_missing_tables.sql` |
| 运行 `clear_stale_sync_state.sql` 清理 | 1 分钟 | `clickhouse-client --multiquery < scripts/clear_stale_sync_state.sql` |
| 运行 `verify_cleanup.py` 验证 | 1 分钟 | `python scripts/verify_cleanup.py` |
| 回填 8 个未规划接口 | ~5 小时 | `python scripts/backfill_unplanned.py --all` |
| 回填 fina_audit（~137K 单元） | ~8-10 小时 | `python scripts/resume_fina_audit.py` |
| 回填 fund_sales（~2800 单元） | ~12 小时 | `python scripts/resume_fund_slow.py` |
| 清理本机 16 个 Python 残留进程 | 10 分钟 | 手动 `tasklist` + 杀进程 |

---

## 五、配置统计（修复后）

| 指标 | 数值 |
|------|------|
| 总接口数 | 228 |
| 启用 | 162 |
| 禁用 | 66 |
| normal 桶 | 126 |
| special 桶 | 102 |
| date_loop 策略 | 129 |
| full_once 策略 | 61 |
| per_symbol 策略 | 18 |
| period_loop 策略 | 5 |
| per_symbol_period 策略 | 6 |
| offset_paging 策略 | 8 |
| monthly_window 策略 | 1 |

---

## 八、测试结果

### 单元测试

| 测试范围 | 通过 | 失败 | 说明 |
|----------|------|------|------|
| 全部单元测试 | 426/427 | 1 | 失败的 `test_share_only_normalizes_for_fund_tables` 与本次修改无关（历史遗留） |
| TushareClient | 18/18 | 0 | 包括新增的连接回收线程安全测试 |
| Planner/ScopeKey | 37/37 | 0 | 规划器和 scope_key 生成全部通过 |
| RateLimiter | 8/8 | 0 | 速率限制器并发测试全部通过 |
| **新增验证** | 4/4 | 0 | HTTP/2 回收逻辑 + 线程安全 + normalize_items |

### 失败项分析

`test_migration_field_resolver.py::test_share_only_normalizes_for_fund_tables` — 测试 `should_normalize("total_share", "tushare_fund_portfolio")` 期望返回 True 但实际返回 False。这是迁移框架的历史遗留问题，与本次修改无关。

### 集成测试

22 个集成测试失败全部因为 **本地未连接 ClickHouse 数据库**，非代码问题。

---

## 九、修改文件清单

### 代码文件（2 个）
- `src/tushare_db/core/tushare_client.py`
- `src/tushare_db/runner/worker.py`

### 配置文件（10 个）
- `config/interfaces/stock_basic.yaml`
- `config/interfaces/stock_daily.yaml`
- `config/interfaces/stock_financial.yaml`
- `config/interfaces/stock_reference.yaml`
- `config/interfaces/stock_special.yaml`
- `config/interfaces/stock_limit_board.yaml`
- `config/interfaces/stock_moneyflow.yaml`
- `config/interfaces/fund.yaml`
- `config/interfaces/index.yaml`
- `config/interfaces/bonds.yaml`
- `config/interfaces/futures.yaml`
- `config/interfaces/bonds.yaml`

### 新建文件（7 个）
- `scripts/backfill_unplanned.py`
- `scripts/resume_fina_audit.py`
- `scripts/resume_fund_slow.py`
- `scripts/create_missing_tables.sql`
- `scripts/clear_stale_sync_state.sql`
- `scripts/verify_cleanup.py`
- `docs/REPORT_20260429.md`（本报告）

---

## 六、接口重新启用（5 个）

> 基于 Tushare Pro 官方 API 文档截图确认以下接口实际存在且可用。

| # | 接口 | 原禁用原因 | 修复内容 | 状态 |
|---|------|-----------|----------|------|
| 1 | stk_week_month_adj | "Returns 0 rows" | 启用，`freq_bucket: special`，`static_params: freq: W` 配置正确 | 已启用 |
| 2 | cn_pmi | "Frequency limit 100/min" | 启用，策略 `date_loop` → `full_once`（与 cn_cpi/cn_ppi 等宏观 API 一致） | 已启用 |
| 3 | tdx_daily | "Empty sample" | 启用，`symbol_source` → `tushare_tdx_index`（需要 TDX 板块代码） | 已启用 |
| 4 | fut_weekly_monthly | "No such API" | 启用，`symbol_source` → `tushare_fut_basic`，`static_params: freq: W` | 已启用 |
| 5 | dc_member | "Consistently times out" | 保持禁用，但修正策略为 `per_symbol` + `symbol_source: tushare_tdx_index`，修复注释 | 待观察 |

### 配置验证

```
Total interfaces: 228
Enabled: 166  (+4)
Disabled: 62  (-4)
```

所有配置通过 pydantic 校验，426/427 单元测试通过。

### 待执行

| 任务 | 说明 |
|------|------|
| stk_week_month_adj 首次回填 | `tushare-db backfill --interface stk_week_month_adj` |
| cn_pmi 首次回填 | `tushare-db backfill --interface cn_pmi` |
| tdx_daily 首次回填 | 需先确保 tdx_index 已入库 |
| fut_weekly_monthly 首次回填 | 需先确保 fut_basic 已入库 |
| dc_member 超时排查 | 需要测试 API 连通性，确认是否仍可调用 |
